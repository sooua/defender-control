---
const specs = ['Rust · MSVC', 'Win32 + GDI', 'DWM borderless', 'TrustedInstaller', '4 languages'];
---

<section class="specbar">
  <div class="container specbar__inner">
    {specs.map((s) => <span class="mono-label">{s}</span>)}
  </div>
</section>
